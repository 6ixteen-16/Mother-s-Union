import HeroImageManager from "@/components/admin/HeroImageManager";

export const metadata = {
  title: "Manage Hero Images | Staff Dashboard",
};

export default function StaffHeroPage() {
  return <HeroImageManager />;
}
