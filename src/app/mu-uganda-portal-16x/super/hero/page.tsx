import HeroImageManager from "@/components/admin/HeroImageManager";

export const metadata = {
  title: "Manage Hero Images | Admin Panel",
};

export default function SuperHeroPage() {
  return <HeroImageManager />;
}
